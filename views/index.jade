extends layout

block content

  .container
    h1 Artciles
    hr
    each article in articles
      a(href=article.url,target='_blank')
        h3  [#{article.source}] #{article.title}
      h4
      p  #{article.description}
      hr
